# 🚀 Брз старт - PHP вграден сервер

## Стартување на проектот

### 1. Отвори командна линија (CMD)

### 2. Навигација до проектот
```bash
cd C:\xampp\htdocs\websites\demo
```

### 3. Стартување на PHP вграден сервер
```bash
php -S localhost:8888
```

**Важно:** Остави ја командната линија отворена додека работиш на проектот!

### 4. Отвори во браузер
```
http://localhost:8888
```

---

## 📋 Проверка на функционалност

### Автоматска проверка

Отвори во браузер:
```
http://localhost:8888/test_functionality.php
```

Ова ќе провери:
- ✅ Конекција со базата податоци
- ✅ Постоење на табели (`app_users`, `notes`)
- ✅ Постоење на `password` колона
- ✅ Session работа
- ✅ Важни датотеки

### Ручно тестирање

#### Чекор 1: Регистрација
1. Отвори: `http://localhost:8888/`
2. Кликни **"Register"**
3. Внеси email и password (минимум 4 карактери)
4. Кликни **"Register"**
5. **Очекувано:** Редирекција на `/login`

#### Чекор 2: Најава
1. На `/login` страницата, внеси email и password
2. Кликни **"Login"**
3. **Очекувано:** Редирекција на `/notes`

#### Чекор 3: Приказ на белешки
1. По најава, треба да си на `/notes` страница
2. **Очекувано:**
   - Навигацијата покажува твојот email
   - Го гледаш "Your Notes" насловот
   - Можеш да кликнеш "Create New Note"

#### Чекор 4: Креирање на белешка
1. Кликни **"Create New Note"**
2. Внеси текст
3. Кликни **"Save Note"**
4. **Очекувано:** Новата белешка е прикажана

---

## 🔧 Проверка на база податоци

### Преку phpMyAdmin

1. Отвори: `http://localhost/phpmyadmin`
2. Избери база: `myapps`

### Проверка дали `password` колоната постои

```sql
SHOW COLUMNS FROM app_users;
```

**Ако НЕ постои `password` колона:**

Изврши го во phpMyAdmin:
```sql
ALTER TABLE app_users ADD COLUMN password VARCHAR(255) NULL;
```

Или изврши го скриптот: `demo/sqldatabase/add_password_column.sql`

---

## ⚠️ Важни напомени

### URL структура

Кога користиш PHP вграден сервер (`php -S localhost:8888`):
- Базен URL: `http://localhost:8888`
- Регистрација: `http://localhost:8888/registration/create`
- Најава: `http://localhost:8888/login`
- Белешки: `http://localhost:8888/notes`

### За да застане серверот

Во командната линија каде што работи серверот, притисни:
```
Ctrl + C
```

---

## 🐛 Можни проблеми

### "Database connection failed"
- Провери дали MySQL работи во XAMPP
- Стартувај Apache и MySQL од XAMPP Control Panel
- Провери `demo/config.php` → `dbname` треба да е `myapps`

### "Table doesn't exist"
- Отвори phpMyAdmin
- Изврши го SQL скриптот: `demo/sqldatabase/myapp.sql`
- Или креирај база и табели рачно

### "404 Not Found" на рути
- Провери дали си на правилен URL: `http://localhost:8888/` (не `http://localhost:8888/index.php`)
- PHP вграден сервер автоматски ги рутира сите барања низ `index.php`

### Session не работи
- PHP вграден сервер автоматски поддржува sessions
- Провери дали cookies се овозможени во браузерот

---

## 📚 Дополнителни ресурси

За подетални упатства, види:
- `TESTING_GUIDE.md` - Детално тестирање
- `test_functionality.php` - Автоматска проверка

---

**✅ Готово! Проектот треба да работи на `http://localhost:8888`**



