# 📋 Водич за тестирање на функционалност

## 1. Почетна проверка

### А) Проверка на база податоци

1. **Отвори phpMyAdmin**: `http://localhost/phpmyadmin`
2. **Провери дали базата `myapps` постои**
3. **Провери дали табелата `app_users` има `password` колона:**
   ```sql
   SHOW COLUMNS FROM app_users;
   ```
   - Ако НЕ постои, изврши го: `demo/sqldatabase/add_password_column.sql`
   
4. **Провери дали табелата `notes` постои**

### Б) Автоматска проверка со тест скрипт

**PHP вграден сервер (php -S localhost:8888):**
```
http://localhost:8888/test_functionality.php
```

**XAMPP Apache:**
```
http://localhost/websites/demo/test_functionality.php
```

Ова ќе провери:
- ✅ Конекција со базата
- ✅ Постоење на табели и колони
- ✅ Session работа
- ✅ Важни датотеки

## 2. Чекор-по-чекор тестирање

### Чекор 1: Регистрација на нов корисник

**PHP вграден сервер:**
1. Отвори: `http://localhost:8888/`

**XAMPP Apache:**
1. Отвори: `http://localhost/websites/demo/`

2. Кликни на **"Register"** во горниот десен агол
3. Внеси:
   - Email: `test@example.com`
   - Password: `test1234` (минимум 4 карактери)
4. Кликни **"Register"**
5. **Очекуван резултат:** Редирекција на `/login` страница

### Чекор 2: Најава на корисник

1. На `/login` страницата, внеси:
   - Email: `test@example.com`
   - Password: `test1234`
2. Кликни **"Login"**
3. **Очекуван резултат:** Редирекција на `/notes` страница

### Чекор 3: Приказ на белешки

1. По успешна најава, треба да бидеш на `/notes` страница
2. **Очекуван резултат:**
   - Навигацијата покажува твојот email наместо "Login/Register"
   - Видиш "Your Notes" наслов
   - Може да кликнеш "Create New Note"

### Чекор 4: Креирање на нова белешка

1. Кликни **"Create New Note"**
2. Внеси текст во полето
3. Кликни **"Save Note"**
4. **Очекуван резултат:** Редирекција на `/notes` со новата белешка во листата

### Чекор 5: Приказ на сите белешки

1. На `/notes` страницата
2. **Очекуван резултат:**
   - Се прикажуваат сите белешки од базата за најавениот корисник
   - Можеш да кликнеш "View", "Edit" или "Delete" на секоја белешка

### Чекор 6: Одјава

1. Кликни **"Logout"** во навигацијата
2. **Очекуван резултат:** Редирекција на почетна страница, навигацијата покажува "Login/Register"

## 3. Проверка на дизајн

✅ **Навигација:**
- Темна позадина (bg-gray-800)
- Сино-виолетов лого (брановидни линии)
- Bell икона за нотификации
- Сино-виолетов "Register" копче (#7c3aed)

✅ **Форми (Register/Login):**
- Светло сива позадина (bg-gray-200)
- Бела форма во центар
- Сино-виолетов лого над насловот
- Сино-виолетов копче за Submit

✅ **Notes страница:**
- Светло сива позадина
- Бели картички за секоја белешка
- Сино-виолетов копче за "Create New Note"

## 4. Можни проблеми и решенија

### Проблем: "Database connection failed"
**Решение:**
- Провери дали MySQL работи во XAMPP
- Провери `demo/config.php` - дали `dbname` е `myapps`
- Провери дали базата `myapps` постои

### Проблем: "Table 'app_users' doesn't exist"
**Решение:**
- Изврши го SQL скриптот: `demo/sqldatabase/myapp.sql`
- Или креирај ги табелите рачно во phpMyAdmin

### Проблем: "Unknown column 'password' in 'app_users'"
**Решение:**
- Изврши го: `demo/sqldatabase/add_password_column.sql`
- Или рачно:
  ```sql
  ALTER TABLE app_users ADD COLUMN password VARCHAR(255) NULL;
  ```

### Проблем: "404 Not Found" на рути
**Решение:**
- Провери `.htaccess` дали е правилно конфигуриран
- Провери дали URL-от е правилен (напр. `/websites/demo/registration/create`)

### Проблем: Session не работи
**Решение:**
- Провери дали `session_start()` се повикува (автоматски преку middleware)
- Провери дали cookies се овозможени во браузерот

## 5. Додатни тестови

### Тест со повеќе корисници

1. Регистрирај уште еден корисник: `user2@example.com`
2. Создај белешки за двата корисници
3. Најави се како првиот корисник
4. **Очекуван резултат:** Го гледаш само своите белешки

### Тест на валидација

1. Покушај да се регистрираш со:
   - Невалиден email → треба да покаже грешка
   - Празно поле → треба да покаже грешка
   - Лозинка помалку од 4 карактери → треба да покаже грешка
2. Покушај да се најавиш со:
   - Погрешен email → треба да покаже грешка
   - Погрешна лозинка → треба да покаже грешка

## 6. Корисни SQL команди за проверка

```sql
-- Прикажи ги сите корисници
SELECT id, email, name FROM app_users;

-- Прикажи ги сите белешки
SELECT id, body, user_id FROM notes;

-- Прикажи белешки за одреден корисник
SELECT * FROM notes WHERE user_id = 1;

-- Провери дали password колоната постои
SHOW COLUMNS FROM app_users LIKE 'password';
```

## 7. Крајно тестирање

После сите чекори, треба да имаш:

✅ Работечка регистрација  
✅ Работечка најава  
✅ Приказ на белешки по корисник  
✅ Креирање на нови белешки  
✅ Правилен дизајн (темна навигација, светло сив фон, сино-виолетов акцент)  
✅ Одјава работи  

---

**За помош:** Отвори `test_functionality.php` за автоматска проверка!

